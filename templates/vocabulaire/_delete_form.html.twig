<form method="post" action="{{ path('vocabulaire_delete', {'id': vocabulaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vocabulaire.id) }}">
    <button class="btn">Delete</button>
</form>
