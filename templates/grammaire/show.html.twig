{% extends 'base.html.twig' %}

{% block title %}Grammaire{% endblock %}

{% block body %}
    <h1>Grammaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grammaire.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ grammaire.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ grammaire.description|raw }}</td>
            </tr>
            <tr>
                <th>Jlpt</th>
                <td>{{ grammaire.jlpt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grammaire_index') }}">back to list</a>

    <a href="{{ path('grammaire_edit', {'id': grammaire.id}) }}">edit</a>

    {{ include('grammaire/_delete_form.html.twig') }}
{% endblock %}
