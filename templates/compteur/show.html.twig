{% extends 'base.html.twig' %}

{% block title %}Compteur{% endblock %}

{% block body %}
    <h1>Compteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compteur.id }}</td>
            </tr>
            <tr>
                <th>Symbole</th>
                <td>{{ compteur.symbole }}</td>
            </tr>
            <tr>
                <th>Sens</th>
                <td>{{ compteur.sens }}</td>
            </tr>
            <tr>
                <th>Tableau</th>
                <td>{{ compteur.tableau|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compteur_index') }}">back to list</a>

    <a href="{{ path('compteur_edit', {'id': compteur.id}) }}">edit</a>

    {{ include('compteur/_delete_form.html.twig') }}
{% endblock %}
